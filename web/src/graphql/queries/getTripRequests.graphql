query GetTripRequests {
  getTripRequests {
    tripRequests {
      id
      custom_name
      dates
      locations {
        name
        id
        legacy_id
        latitude
        longitude
        parent_name
        subparent_id
      }
      active
      min_nights
      type
    }
  }
}
